import { NextResponse } from "next/server"; import { transporter, mailOptions } from "@/lib/nodemailer"; export async function GET() { try { const info = await transporter.sendMail({ ...mailOptions, to: "moores1807@gmail.com", subject: "Vercel Email Test", text: "Testing if Vercel can send emails via Gmail SMTP." }); return NextResponse.json({ success: true, info }); } catch (error: any) { return NextResponse.json({ success: false, error: error.message, stack: error.stack }, { status: 500 }); } }